export function PruebaBoton({user, name}) {
    return (<>
        <img src="" alt="" />
        <div>
            <strong>{name}</strong>
            <span>@{user}</span>
        </div>
        <button>seguir</button>

    </>)
}